export type FormValues = {
  title: string;
  description: string;
};

export type State = {
  tiles: [];
};

export type NewTile = {
  createdAt: string;
  description: string;
  id: string;
  title: string;
};
